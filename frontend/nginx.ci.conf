# CI uses a static-only frontend config so the smoke test validates the built UI without any backend proxying.
server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }
}
